version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    # Amplify will deploy the built Next.js app
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**
